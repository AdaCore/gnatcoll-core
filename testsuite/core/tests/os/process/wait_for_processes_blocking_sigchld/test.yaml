title: Test that wait_for_process works even when sigchld is blocked by the caller
data: [ "test_data/*.py" ]
control:
- [ SKIP, "env.is_cross", "Test uses python which is not available on cross targets" ]
- [ SKIP, "env.build.os.name == 'windows'", "SIGCHLD signal does not exist on windows" ]
