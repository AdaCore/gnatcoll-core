description: >
  When replacing a composite attribute value make sure
  that no trailing parts of previous concatenation
  remain in place.
data:
    - "p.gpr"
control:
    - [SKIP, "env.is_cross", "Libutil missing from cross-linux sysroots, see T616-039"]
