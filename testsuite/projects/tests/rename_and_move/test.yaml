description: Test Rename_And_Move and ensure that trailing directory separator are kept
data:
    - "p.gpr"
    - "prj.gpr"
    - "test.adb"
    - "src"
    - "src/subsrc"
    - "src/subsrc/main.adb"
control:
    - [SKIP, "env.is_cross", "GNATCOLL.Projects is not built for cross targets"]
