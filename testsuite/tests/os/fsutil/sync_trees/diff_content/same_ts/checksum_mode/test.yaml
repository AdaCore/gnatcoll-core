description: Test for GNATCOLL.OS.FSutil.Sync_Trees. Scenario with mode set to
             CHECKSUM and with files having the same timestamps but different
             content
driver: build_run_assert_and_diff
data:
  - "src_top_dir"
  - "dst_top_dir"
source_dirs:
  - "../../../"