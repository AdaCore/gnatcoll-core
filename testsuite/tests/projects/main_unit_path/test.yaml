description: >
  Check that Is_Main_Unit correctly handles
  absolute and relative path names.
data:
    - "foo1.gpr"
    - "foo.adb"
control:
    - [SKIP, "env.is_cross", "Libutil missing from cross-linux sysroots, see T616-039"]
