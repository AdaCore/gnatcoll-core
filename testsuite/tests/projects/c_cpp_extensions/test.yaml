description: >
  Check default extensions for C and C++ in case when
  autoconf is not enabled and naming scheme comes from
  predefined in-memory cgpr.
data:
    - "p.gpr"
control:
    - [SKIP, "env.is_cross", "Libutil missing from cross-linux sysroots, see T616-039"]
