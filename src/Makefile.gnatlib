MODULE=gnatlib
PROJECT=gnatlib
LIBNAME=lib${TARNAME}

ifeq ($(WITH_SYSLOG),yes)
SRCDIRS=./ ./syslog/
else
SRCDIRS=./ ./nosyslog/
endif

include ../Makefile.conf
all:     buildall
clean:   cleanall
install: libinstall

